/* This script demonstrates two things:
 * 
 * 1. How to detect if the player is within a certain area of the world
 * 2. How to play a sound
 * 
 * This script could be placed on the FPS controller or on some empty object ......
 */



using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

//public class Beep : MonoBehaviour {

	// create a GameObject to represent the player
//	GameObject playerObject;
//	public Text scoreCount;
//	private int score;

	// create an Audioclip object. This assumes we drag a sound file onto the myclip box in the Inspector.
//	public AudioClip myclip;

	// Use this for initialization
//	void Start () {

		// associate playerObject with the player. This assumes the First Person Controller is name "player
//		playerObject = GameObject.Find ("FPSController");

		// get the actual Sound file from the Inspector
//		GetComponent<AudioSource>().clip = myclip;
//		score = 0;
//		scoreCounter ();
//	}

	// Update is called once per frame
//	void Update () {

		// play the sound if the player is outside some boundaries

//		if (playerObject.transform.position.x > 780 ||playerObject.transform.position.x == 780) {
//			GetComponent<AudioSource> ().PlayOneShot(myclip);
//			score++;
//			scoreCounter ();
//		}

	//	if (playerObject.transform.position.x < -780) {
		//	GetComponent<AudioSource> ().Play (1000);
//}



		
	//}
	//void scoreCounter () 
	//{
	//	scoreCount.text = "Score = " + score.ToString ();

	//}
//}
